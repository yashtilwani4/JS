# HTML & JavaScript Basics – Core Notes

This branch contains rough notes focused on how HTML and JavaScript work together, especially script loading, DOM readiness, and essential meta tags.

## Topics Covered

### 1. `<script>` Tag Basics
- Purpose of the `<script>` tag  
- Inline script vs external JS file  
- Behavior of multiple script tags  

### 2. Script Loading & Blocking
- What blocking means in browser rendering  
- How JavaScript blocks HTML parsing  
- Impact of blocking scripts on page load  

### 3. `defer` Attribute
- What `defer` does  
- When and why to use `defer`  
- Execution after HTML parsing  

### 4. `async` Attribute
- `async` vs `defer`  
- When `async` is useful  
- Common risks and mistakes with `async`  

### 5. Script Placement
- Script inside `<head>`  
- Script at bottom of `<body>`  
- Modern best practice: using `defer` in `<head>`  
- Comparison and reasoning  

### 6. DOM Readiness
- Why JS sometimes fails to access elements  
- `DOMContentLoaded` vs `window.onload`  
- Safe ways to run JS after DOM is ready  

### 7. Meta Tags – Basics
- What meta tags are  
- Why meta tags matter  
- Proper placement of meta tags  

### 8. Important Meta Tags
- `charset`  
- `viewport`  
- `description`  
- `robots`  
- Basic SEO understanding  

📌 Note:  
These are rough study notes pushed under the `html-advance-tags` branch for learning and revision.
